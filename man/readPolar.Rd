% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-polar.R
\name{readPolar}
\alias{readPolar}
\title{Read a Polar heart-rate / R-R interval export}
\usage{
readPolar(path, sep = ",")
}
\arguments{
\item{path}{Path to a Polar \code{.csv}/\code{.txt} export.}

\item{sep}{Field separator (default: comma).}
}
\value{
A \code{PhysioExperiment}; R-R intervals are available via
\code{\link[PhysioCore:getEvents]{PhysioCore::getEvents()}}.
}
\description{
Reads a Polar (Flow / H10) export into a \code{PhysioExperiment}. A heart-rate
column (bpm) becomes the \code{"raw"} assay; R-R intervals (ms) are attached as
\link[PhysioCore:PhysioEvents]{PhysioCore::PhysioEvents} (one event per beat, with onset at the cumulative
R-R time and the interval in the value). When only R-R intervals are present,
instantaneous heart rate (60000 / R-R) is used as the signal.
}
\examples{
f <- system.file("extdata", "polar_hr.csv", package = "PhysioDevices")
if (nzchar(f)) {
  pe <- readPolar(f)
  PhysioCore::nEvents(PhysioCore::getEvents(pe))
}
}
\references{
Polar. "Flow / H10 heart-rate and R-R export."
}
\seealso{
\code{\link[=readEmpaticaE4]{readEmpaticaE4()}}, \code{\link[=readShimmer]{readShimmer()}}
}
