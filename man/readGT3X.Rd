% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-actigraph.R
\name{readGT3X}
\alias{readGT3X}
\title{Read an ActiGraph GT3X accelerometer file}
\usage{
readGT3X(path, imputeZeroes = TRUE)
}
\arguments{
\item{path}{Path to a \code{.gt3x} file.}

\item{imputeZeroes}{If \code{TRUE} (default) idle-sleep-mode gaps are filled with
zeroes (passed to \code{\link[read.gt3x:read.gt3x]{read.gt3x::read.gt3x()}}) so the series stays regularly
sampled.}
}
\value{
A \code{PhysioExperiment} with an \code{acceleration} assay (x/y/z, g) at the
device sample rate; device metadata in \code{metadata()}.
}
\description{
Reads a raw ActiGraph \code{.gt3x} file (the common US physical-activity /
actigraphy device format) into a tri-axial-acceleration \code{PhysioExperiment}.
The binary decode is done by the \pkg{read.gt3x} package; the result is the
same shape as \code{\link[PhysioWearable:accelToPhysioExperiment]{PhysioWearable::accelToPhysioExperiment()}}, so it feeds the
free-living accelerometry pipeline (\code{computeENMO()}, \code{summarizeFreeLiving()}).
}
\examples{
\dontrun{
pe <- readGT3X("subject.gt3x")
acc <- SummarizedExperiment::assay(pe, "acceleration")
PhysioWearable::computeENMO(acc, sampling_rate = samplingRate(pe))
}
}
\references{
ActiGraph GT3X format; Neishabouri A et al. read.gt3x.
}
\seealso{
\code{\link[=readCWA]{readCWA()}}, \code{\link[PhysioWearable:readAccelCSV]{PhysioWearable::readAccelCSV()}}
}
