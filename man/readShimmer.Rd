% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-shimmer.R
\name{readShimmer}
\alias{readShimmer}
\title{Read a Shimmer GSR3+ Consensys CSV export}
\usage{
readShimmer(path, sep = NULL)
}
\arguments{
\item{path}{Path to a Shimmer Consensys \code{.csv} file.}

\item{sep}{Field separator. By default it is taken from a leading
\verb{sep=<char>} line, falling back to a tab.}
}
\value{
A \code{PhysioExperiment} with one channel per calibrated signal.
}
\description{
Reads a Shimmer GSR3+ Consensys export (GSR / skin conductance, PPG,
accelerometer) into a \code{PhysioExperiment}. The calibrated (\verb{_CAL}) columns are
read in their physical units; the timestamp column determines the sampling
rate; and each channel's unit (from the export's unit row) is stored in the
column metadata.
}
\examples{
f <- system.file("extdata", "shimmer_gsr3.csv", package = "PhysioDevices")
if (nzchar(f)) {
  pe <- readShimmer(f)
  channelNames(pe)
}
}
\references{
Shimmer. "Consensys — data export format."
}
\seealso{
\code{\link[=readEmpaticaE4]{readEmpaticaE4()}}, \code{\link[=readPolar]{readPolar()}}
}
