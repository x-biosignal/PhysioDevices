% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-googlefit.R
\name{readGoogleFit}
\alias{readGoogleFit}
\title{Read a Google Fit (Google Takeout) export}
\usage{
readGoogleFit(path, what = NULL, tz = "UTC")
}
\arguments{
\item{path}{Directory of the extracted Takeout "Fit" folder (searched
recursively), or a file / vector of files.}

\item{what}{Optional restriction, any of \code{"heart_rate"}, \code{"steps"}, \code{"daily"}.}

\item{tz}{Time zone for parsed timestamps (default \code{"UTC"}).}
}
\value{
A \code{google_fit} object: a list with any of \code{heart_rate}, \code{steps}
(\code{time}, \code{value}) and \code{daily} (per-day summary), plus \code{path}/\code{tz}.
}
\description{
Parses the "Fit" folder of a Google Takeout archive. From "All Data" it reads
the granular per-type JSON points -- heart rate (\code{heart_rate.bpm}) and steps
(\code{step_count.delta}) -- decoding their nanosecond timestamps; from "Daily
activity metrics" it reads the daily-summary CSVs (steps, calories, distance,
average/max/min heart rate).
}
\examples{
\dontrun{
gf <- readGoogleFit("Takeout/Fit", tz = "Asia/Tokyo")
gf$heart_rate
}
}
\references{
Google Takeout "Fit" export; Google Fit data types.
}
\seealso{
\code{\link[=readHealthConnect]{readHealthConnect()}}, \code{\link[=readFitbit]{readFitbit()}}
}
