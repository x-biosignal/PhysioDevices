% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-applehealth.R
\name{readAppleHealth}
\alias{readAppleHealth}
\title{Read an Apple Health export}
\usage{
readAppleHealth(path, types = NULL, tz = "UTC")
}
\arguments{
\item{path}{Path to the Apple Health \code{export.xml}.}

\item{types}{Optional character vector of (prefix-stripped) record types to
keep; \code{NULL} (default) keeps all.}

\item{tz}{Time zone for the parsed timestamps (default \code{"UTC"}).}
}
\value{
An \code{apple_health} object: a list with \code{records} (a data frame with
\code{type}, \code{source}, \code{unit}, \code{start}, \code{end}, \code{value}, \code{value_num}), \code{workouts}
(or \code{NULL}), \code{path} and \code{tz}.
}
\description{
Parses an Apple Health \code{export.xml} into a tidy set of records and workout
summaries. Every sample Apple stores as a \verb{<Record>} -- heart rate, heart-rate
variability (\code{HeartRateVariabilitySDNN}), blood oxygen (\code{OxygenSaturation}),
respiratory rate, step count, active/basal energy, resting and walking heart
rate, VO2 max, and \code{SleepAnalysis} sleep stages -- becomes one row; \verb{<Workout>}
elements become the \code{workouts} table. The reader streams the file, so multi-
hundred-MB exports are handled without loading the whole document into memory.
}
\details{
The Apple type prefix (\code{HKQuantityTypeIdentifier} / \code{HKCategoryTypeIdentifier})
is stripped, so \code{type} reads e.g. \code{"HeartRate"}, \code{"OxygenSaturation"},
\code{"SleepAnalysis"}. Numeric samples are parsed into \code{value_num}; category
samples (sleep stages) keep their (prefix-stripped) label in \code{value}.
}
\examples{
\dontrun{
ah <- readAppleHealth("apple_health_export/export.xml", tz = "Asia/Tokyo")
appleHealthTypes(ah)
hr <- appleHealthSeries(ah, "HeartRate")
}
}
\references{
Apple Inc. HealthKit data types and the Health app XML export.
}
\seealso{
\code{\link[=appleHealthSeries]{appleHealthSeries()}}, \code{\link[=appleHealthTypes]{appleHealthTypes()}}, \code{\link[=appleHealthExperiment]{appleHealthExperiment()}},
\code{\link[=readAppleECG]{readAppleECG()}}
}
