% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-healthconnect.R
\name{readHealthConnect}
\alias{readHealthConnect}
\title{Read an Android Health Connect CSV export}
\usage{
readHealthConnect(path, tz = "UTC")
}
\arguments{
\item{path}{Directory of the CSV export (searched recursively), or a file /
vector of files.}

\item{tz}{Time zone for parsed timestamps (default \code{"UTC"}).}
}
\value{
A \code{health_connect} object: a list with any of \code{heart_rate}, \code{steps},
\code{spo2}, \code{respiratory_rate} (\code{time}, \code{value}), and \code{sleep} (stage intervals
\code{start}, \code{end}, \code{stage}), plus \code{path}/\code{tz}. \code{spo2} feeds
\code{PhysioWearable::spo2Metrics()}, \code{sleep} feeds
\code{PhysioWearable::summarizeSleepStages()}.
}
\description{
Parses a directory of Health Connect record-type CSVs (one file per type) into
tidy per-modality series. Recognises \code{HeartRate}, \code{Steps}, \code{OxygenSaturation},
\code{RespiratoryRate} and \code{SleepSession} (its stages) by filename; the time and
value columns are found by name heuristics, so the common exporter layouts
work without configuration.
}
\examples{
\dontrun{
hc <- readHealthConnect("health_connect_export", tz = "Asia/Tokyo")
hc
PhysioWearable::spo2Metrics(hc$spo2$value, time = hc$spo2$time)
}
}
\references{
Android Health Connect data types and export.
}
\seealso{
\code{\link[=readGoogleFit]{readGoogleFit()}}, \code{\link[=readFitbit]{readFitbit()}}
}
