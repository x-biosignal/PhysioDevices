% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-oura.R
\name{readOura}
\alias{readOura}
\title{Read an Oura Ring export}
\usage{
readOura(path, tz = "UTC")
}
\arguments{
\item{path}{Path to the Oura sleep JSON (a v2 object with a \code{data} array, a v1
object with a \code{sleep} array, or a bare array of sleep periods).}

\item{tz}{Time zone for the parsed timestamps (default \code{"UTC"}).}
}
\value{
An \code{oura} object: a list with \code{sleep} (stage intervals \code{start}, \code{end},
\code{stage}, \code{day}), \code{sleep_summary} (per-day \code{efficiency},
\code{total_sleep_min}, \code{time_in_bed_min}), \code{heart_rate} (\code{time}, \code{bpm}) and
\code{hrv} (\code{time}, \code{rmssd}), plus \code{path}/\code{tz}. The \code{sleep} stages feed
\code{PhysioWearable::summarizeSleepStages()}.
}
\description{
Parses an Oura sleep export/JSON (API v2 \code{sleep} collection, or the older API
v1 \code{sleep}) into tidy per-night data: sleep-stage intervals (from the 5-minute
phase string), the 5-minute heart-rate and HRV (RMSSD) series, and Oura's own
sleep summary. Both API versions are recognised.
}
\examples{
\dontrun{
ou <- readOura("oura_sleep.json", tz = "Asia/Tokyo")
PhysioWearable::summarizeSleepStages(ou$sleep,
  asleep_levels = c("light", "deep", "rem"))
}
}
\references{
Oura API v2 sleep data.
}
\seealso{
\code{\link[=readFitbit]{readFitbit()}}, \code{\link[=readHealthConnect]{readHealthConnect()}}
}
