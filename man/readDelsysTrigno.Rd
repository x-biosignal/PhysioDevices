% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-delsys.R
\name{readDelsysTrigno}
\alias{readDelsysTrigno}
\title{Read a Delsys Trigno CSV export}
\usage{
readDelsysTrigno(path, sep = ",")
}
\arguments{
\item{path}{Path to a Trigno \code{.csv} export.}

\item{sep}{Field separator (default: comma).}
}
\value{
A \link[PhysioCore:MultiRatePhysioExperiment]{PhysioCore::MultiRatePhysioExperiment} with an \code{"emg"} stream and,
when present, an \code{"imu"} stream.
}
\description{
Parses a Delsys Trigno File Utility / EMGworks CSV export into a
\link[PhysioCore:MultiRatePhysioExperiment]{PhysioCore::MultiRatePhysioExperiment}. EMG channels (high rate) and the IMU
channels (accelerometer / gyroscope / magnetometer, lower rate) are separated
into their own streams, each at the rate implied by its per-channel \code{X[s]}
time column, with per-sensor column metadata (sensor number and modality).
}
\examples{
f <- system.file("extdata", "delsys_trigno.csv", package = "PhysioDevices")
if (nzchar(f)) {
  mr <- readDelsysTrigno(f)
  PhysioCore::streamNames(mr)
}
}
\references{
Delsys. "Trigno File Utility / EMGworks export."
}
\seealso{
\code{\link[=readXsensMVNX]{readXsensMVNX()}}, \code{\link[=readBIOPAC]{readBIOPAC()}}
}
