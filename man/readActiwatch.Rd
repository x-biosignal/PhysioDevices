% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-actiwatch.R
\name{readActiwatch}
\alias{readActiwatch}
\title{Read an Actiwatch AWD actigraphy file}
\usage{
readActiwatch(path, epoch = 60, tz = "UTC")
}
\arguments{
\item{path}{Path to a \code{.awd} file.}

\item{epoch}{Epoch length in seconds (default 60). The \code{.awd} header does not
reliably carry it, so set it to your recording's epoch.}

\item{tz}{Time zone for the timestamps (default \code{"UTC"}).}
}
\value{
A data frame with \code{time} (\code{POSIXct}) and \code{activity} (counts), with the
epoch length in \code{attr(, "epoch_sec")}. Feeds
\code{PhysioWearable::coleKripke()} (which expects 1-minute epochs).
}
\description{
Parses a Philips/Respironics Actiwatch \code{.awd} file into per-epoch activity
counts with timestamps. The start date/time is read from the header; the
activity counts are the longest run of numeric lines (the data block).
}
\examples{
\dontrun{
aw <- readActiwatch("subject.awd", epoch = 60)
sw <- PhysioWearable::coleKripke(aw$activity)
}
}
\references{
Philips Respironics Actiwatch / Actiware AWD format.
}
\seealso{
\code{\link[=readGT3X]{readGT3X()}}, \code{\link[=readGENEActiv]{readGENEActiv()}}
}
