% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-tcx.R
\name{readTCX}
\alias{readTCX}
\title{Read a TCX (Garmin Training Center) workout file}
\usage{
readTCX(path, tz = "UTC")
}
\arguments{
\item{path}{Path to a \code{.tcx} file.}

\item{tz}{Time zone for the parsed timestamps (default \code{"UTC"}).}
}
\value{
A data frame of trackpoints (\code{time}, \code{lat}, \code{lon}, \code{altitude_m},
\code{distance_m}, \code{hr}, \code{cadence}, \code{speed}), time-ordered, with the activity
\code{sport} in \code{attr(, "sport")}.
}
\description{
Parses the trackpoints of a \code{.tcx} workout (the common Garmin Connect / Strava
export) into a tidy data frame: time, position, altitude, distance, heart rate
and cadence/speed where present.
}
\examples{
\dontrun{
tp <- readTCX("run.tcx")
plot(tp$lon, tp$lat)     # the route
}
}
\references{
Garmin Training Center XML (TCX) schema.
}
\seealso{
\code{\link[=readGPX]{readGPX()}}, \code{\link[=readFitbit]{readFitbit()}}
}
