% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-fitbit.R
\name{readFitbit}
\alias{readFitbit}
\title{Read a Fitbit / Google Pixel data archive}
\usage{
readFitbit(path, what = NULL, tz = "UTC")
}
\arguments{
\item{path}{Directory of the extracted archive (searched recursively), or a
file / vector of files.}

\item{what}{Optional character vector restricting which modalities to read
(any of \code{"heart_rate"}, \code{"steps"}, \code{"spo2"}, \code{"resting_heart_rate"},
\code{"sleep"}); \code{NULL} (default) reads all that are found.}

\item{tz}{Time zone for the parsed timestamps (default \code{"UTC"}).}
}
\value{
A \code{fitbit} object: a list with any of \code{heart_rate}, \code{steps}, \code{spo2},
\code{resting_heart_rate} (data frames of \code{time}, \code{value}), \code{sleep} (stage
intervals \code{start}, \code{end}, \code{stage}, \code{log_id}) and \code{sleep_summary} (Fitbit's
per-log \code{efficiency}, \code{minutes_asleep}, \code{time_in_bed}, ...), plus \code{path}/\code{tz}.
The \code{sleep} stages feed \code{PhysioWearable::summarizeSleepStages()}; \code{spo2} feeds
\code{PhysioWearable::spo2Metrics()}.
}
\description{
Parses a Fitbit account-export (or Google Takeout) directory into tidy
per-modality series. Files are discovered by name and both layouts are
handled: the account archive's per-day files (\code{heart_rate-YYYY-MM-DD.json},
\verb{steps-*}, \verb{sleep-*}, ...) and the Web-API / research extraction's aggregated
single files (\code{heart_rate.json}, \code{steps.json}, \code{sleep.json}; e.g. the PMData
dataset). Modalities: heart rate (\code{bpm}), steps, \code{resting_heart_rate}, SpO2
(\verb{spo2*}/\verb{*oxygen*}, JSON or minute CSV), and sleep (Fitbit's own
\code{wake}/\code{light}/\code{deep}/\code{rem} stages). Google Pixel Watch health data is stored
in Fitbit, so the same reader covers it.
}
\examples{
\dontrun{
fb <- readFitbit("Takeout/Fitbit", tz = "Asia/Tokyo")
fb
PhysioWearable::spo2Metrics(fb$spo2$value, time = fb$spo2$time)
}
}
\references{
Fitbit Web API data types; Fitbit account data export.
}
\seealso{
\code{\link[=readAppleHealth]{readAppleHealth()}}, \code{\link[=fitbitSeries]{fitbitSeries()}}
}
